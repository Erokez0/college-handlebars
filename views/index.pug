extends layouts/layout.pug 

block body
        h1 Main
        p 
            small 
                i #{getTime()}
        h2 Messages:
        ul(class="list bg-base-100 rounded-box shadow-md")
            each message in messages
                li
                    pre=message
        p #{toUppercase(content)}

